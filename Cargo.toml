[workspace]
members = [
    "macro",
]
resolver = "2"

[workspace.lints.rust]
unsafe_code = "forbid"

[workspace.package]
authors = ["Igor Novikov <septagramm@gmail.com>"]
edition = "2024"
license = "Apache-2.0"
keywords = ["ai", "agents", "llm", "chat"]

[[bin]]
name = "artilect"
required-features = ["auth-front", "auth-out", "chat-front", "chat-out"]

[[bin]]
name = "artilect-cortex"
path = "src/bin/artilect_cortex.rs"
required-features = ["auth-in", "chat-in", "telegram-in", "server-http2"]

[[bin]]
name = "artilect-minicortex"
path = "src/bin/artilect_minicortex.rs"
required-features = ["auth-in", "auth-front", "chat-in", "chat-front", "telegram-in"]

[[bin]]
name = "auth"
required-features = ["auth-in", "server-http2"]

[[bin]]
name = "auth-front"
required-features = ["auth-front", "auth-out"]

[[bin]]
name = "chat"
required-features = ["chat-in", "server-http2"]

[[bin]]
name = "chat-front"
required-features = ["chat-front", "chat-out"]

[[bin]]
name = "telegram"
required-features = ["telegram-in", "auth-out", "server-http2"]

[package]
name = "artilect"
version = "0.1.0"
# default-run = "artilect"
authors.workspace = true
edition.workspace = true
license.workspace = true
keywords.workspace = true

[features]
backend = [
    "dep:sqlx",
    "dep:actix",
    "dep:anymap3",
    "dep:dashmap",
    "dep:humantime",
]
server-http2 = [
    "backend",
    "dep:axum",
    "dep:axum-extra",
    "dep:http",
    "dep:headers",
    "dep:tower-http",
    "dep:jsonwebtoken",
    "dep:rand",
]
client = []
client-http2 = ["client", "dep:cookie", "dep:reqwest"]
frontend = [
    "dioxus",
    "dioxus/router",
    "client-http2",
    "dep:classnames",
    "dep:futures-util",
    "dep:dotenvy_macro",
    "time/wasm-bindgen",
    "dep:markdown",
]
infer = [
    "dep:markup",
    "dep:reqwest",
    "dep:serde_yaml",
    "dep:textwrap",
]
keyring-cookie-store = ["dep:cookie_store"]

auth = []
auth-in = ["auth", "backend"]
auth-out = ["auth", "client-http2"]
auth-front = ["frontend"]
chat = []
chat-in = ["chat", "backend", "infer"]
chat-out = ["chat", "client-http2"]
chat-front = ["frontend"]
telegram = []
telegram-in = ["telegram", "backend", "dep:teloxide"]
telegram-out = ["telegram", "client-http2"]

native = []
desktop = ["dioxus/desktop", "dep:tao", "keyring-cookie-store", "native"]
mobile = ["dioxus/mobile", "keyring-cookie-store", "native"]

[dependencies]
artilect-macro = { path = "macro" }
bon = "3.8"
cfg_block = "0.2.0"
derive_more = { version = "2.1", features = ["from"] }
indoc = "2.0"
ouroboros = { version = "0.18" }
tokio = { version = "1.47", features = ["full"] }
parking_lot = { version = "0.12", features = ["nightly"] }
regex = "1.12"
serde = { version = "1.0", features = ["derive"] }
serde_json = { version = "1.0" }
uuid = { version = "1.13", features = ["v4", "serde", "js", "fast-rng"] }
time = { version = "0.3", features = [
    "serde",
    "parsing",
    "formatting",
    "local-offset",
] }
tracing = "0.1"
tracing-subscriber = "0.3"
dotenvy = "0.15"
once_cell = "1.21"
anyhow = "1.0"
itertools = "0.14"
thiserror = "2.0"
keyring = { version = "3.6", features = ["linux-native-async-persistent", "windows-native", "apple-native"] }
url = "2.5"

anymap3 = { version = "1.0.1", optional = true }
actix = { version = "0.13", optional = true }
dioxus = { version = "0.6", optional = true }
futures-util = { version = "0.3", optional = true }
classnames = { version = "2.1", optional = true }
markup = { version = "0.15", optional = true }
markdown = { version = "1.0", optional = true }
rand = { version = "0.9", optional = true }
serde_yaml = { version = "0.9", optional = true }
textwrap = { version = "0.16", features = ["terminal_size"], optional = true }
reqwest = { version = "0.12", optional = true, features = ["json", "cookies"] }
cookie = { version = "0.18", optional = true }
cookie_store = { version = "0.22", optional = true, features = ["serde_json"] }
dotenvy_macro = { version = "0.15", optional = true }
tao = { version = "0.30", optional = true }
axum = { version = "0.8", optional = true, features = ["macros"] }
axum-extra = { version = "0.10", optional = true, features = ["cookie"] }
jsonwebtoken = { version = "10.0", features = ["rust_crypto"], optional = true }
http = { version = "1.2", optional = true }
headers = { version = "0.4", optional = true }
tower-http = { version = "0.5", optional = true, features = ["cors"] }
teloxide = { version = "0.17", optional = true, features = ["macros"] }
humantime = { version = "2.3", optional = true }
dashmap = { version = "6.1", features = ["inline"], optional = true }
sqlx = { version = "0.8", optional = true, features = [
    "runtime-tokio",
    "postgres",
    "uuid",
    "time",
] }

[profile]

[profile.wasm-dev]
inherits = "dev"
opt-level = 1

[profile.server-dev]
inherits = "dev"

[profile.android-dev]
inherits = "dev"
